# PROJECT CLONING 🚀

To view the following project, you can open the file named index.html, which will show you the landing page. To obtain this file, you will need to do the following:

- Create an empty folder.

- Inside the folder, initialize Git with the command: "git init"

- Once there, you can run the command: "git clone https://github.com/Cayuro/Cayuro.git" which will clone the repository where this page is located.

- U can see a branch - (a folder) - with the name Prueba_HTML_CSS, there u can click and open, here u can see a file HTML with the name index.html, and the ecommerce-styles.css tjat is the name of the landing-page that we use for the Style with CSS program.

# PROJECT VISUALIZATION

- To visualize this project, once you have cloned it to the folder you designated for this purpose, you can proceed to navigate to the index.html file. This file will allow you to view the page, either by opening it directly or using a compiler that allows you to see not only the code but also what is generated, such as "live server" in VS Code. You can observe the landing page project.

# CODER INFORMATION --- CAYURO ----

- ​​I am a developer currently learning more about various programming languages ​​such as HTML, CSS, and JavaScript for the frontend, and Python, C#, and Java for the backend.

# RESOURCES USED

- All the resources used are located in the assets folder. These consist of images that allow the landing page to be generated correctly.

# THE DESIGN

- It uses semantic HTML with comments that will guide you to what you want to edit, if needed.

- It's a responsive design, adapting to the screen size depending on the user.

- It includes interactive hover effects for a better user experience.

- The files are structured so that the images are organized in the assets file.